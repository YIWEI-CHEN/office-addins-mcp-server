# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: office-addins-mcp-server
metadata:
  template: office-addins-mcp-server@0.0.1-beta
services:
  api:
    project: .
    host: appservice
    language: python
infra:
  provider: bicep
  path: infra